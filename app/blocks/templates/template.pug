include ../../../node_modules/bemto.pug/bemto.pug
include ../page/page

doctype html
html
        include ../head/head
        +page(class=pageMod ? '_' + pageMod : '')
                .page__wrapper

        +scripts(['app.min.js'])